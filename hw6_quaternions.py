from sympy import *
init_printing(use_unicode=True)
import numpy as np

theta1 = np.pi/6
theta2 = np.pi/3
a1 = np.array([1,0,0]).reshape(3,1)
a2 = np.array([0,0,1]).reshape(3,1)

Matrix([cos(theta1 / 2), 1, 2])

#sin(theta1 / 2) *a1
